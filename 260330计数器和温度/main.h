#ifndef main_h
#define main_h

#include <stdio.h>

#define APP_TITLE "---- 循环练习复读机----\n"

#endif

/* 这里定义了 main_h ，问题是，后续的main.c里并没有用到main_h， 是否理解为，定义的这个main_h，只是划出了一个空间，让整个main.h里面#define的，#include的，各种内容放在里面，所以不用在main.c里面出现？*/
